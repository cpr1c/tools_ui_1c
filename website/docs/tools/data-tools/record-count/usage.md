---
sidebar_position: 3
---

# Использование

## Быстрый старт

1. Перейдите в меню **Универсальные инструменты → Количество объектов в базе**.
2. Выберите вид объектов для подсчёта: справочники, документы, регистры или все.
3. Нажмите **Подсчитать**.
4. Просмотрите результат — таблицу с количеством записей по каждому объекту.

## Сценарии

### Подсчёт количества по всем объектам

```bsl
// Программный аналог расчёта количества
Для Каждого МД Из Метаданные.Справочники Цикл
    Запрос = Новый Запрос;
    Запрос.Текст = "ВЫБРАТЬ КОЛИЧЕСТВО(*) КАК Кол FROM " + МД.ПолноеИмя();
    Результат = Запрос.Выполнить().Выбрать();
    Если Результат.Следующий() Тогда
        Сообщить(МД.Имя + ": " + Результат.Кол);
    КонецЕсли;
КонецЦикла;
```

### Анализ регистров накопления

```bsl
// Подсчёт оборотов по регистрам накопления
Для Каждого МД Из Метаданные.РегистрыНакопления Цикл
    Запрос = Новый Запрос;
    Запрос.Текст = "ВЫБРАТЬ КОЛИЧЕСТВО(*) ИЗ " + МД.ПолноеИмя();
    // Выполнить и вывести результат
КонецЦикла;
```

### Экспорт результатов

```bsl
// После получения результатов в обработке
ТабДок = ЭлементыФормы.Результат.Выгрузить();
ТабДок.Записать("Количество_объектов.xlsx", ТипФайлаТабличногоДокумента.XLSX);
```
